# UFC website simplified 

## How to start project for dev 
- Create dev environment with command `python -m venv .venv`
- Activate dev environment
- Install required python packages with command `pip install -r requirements.txt`
- cd into app directory
- Run with command `flask run`



